version: "5.0.0"

services:
  mongodb:
    image: mongo:6.0.27-jammy
    container_name: mongodb
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db
    restart: always

volumes:
  mongo-data:
